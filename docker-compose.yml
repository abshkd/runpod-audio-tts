# Local testing only
version: "3.8"

services:
  tts:
    build: .
    environment:
      - LOCAL_TEST=1
    ports:
      - "8000:8000"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
